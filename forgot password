<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f9f9f9;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        form {
            border: 3px solid #f1f1f1;
            max-width: 400px;
            padding: 20px;
            background-color: #fff;
            text-align: center;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }
        input[type="email"] {
            width: 100%;
            padding: 12px 20px;
            margin: 8px 0;
            display: inline-block;
            border: 1px solid #ccc;
            box-sizing: border-box;
        }
        button {
            background-color: #0457aa;
            color: white;
            padding: 14px 20px;
            margin: 8px 0;
            border: none;
            cursor: pointer;
            width: 100%;
            border-radius: 5px;
        }
        button:hover {
            opacity: 0.8;
        }
        .imgcontainer {
            text-align: center;
            margin: 24px 0 12px 0;
        }
        img.avatar {
            width: 40%;
            border-radius: 50%;
        }
        h2 {
            margin-bottom: 16px;
        }
        p.instruction {
            font-size: 14px;
            color: #666;
            margin-bottom: 20px;
        }
        .back-to-login {
            text-align: center;
            margin-top: 16px;
        }
    </style>
</head>
<body>

    <!-- Forgot Password Form -->
    <form id="forgotPasswordForm" action="https://example.com/forgot-password" method="post">
        <div class="imgcontainer">
            <img src="bluestock.jpg.png" alt="bluestock" class="avatar">
        </div>
        <h2>Forgot Password?</h2>
        <p class="instruction">Enter your email address to get the password reset link.</p>
        <div class="container">
            <label for="email"><b>Email Address</b></label>
            <input type="email" placeholder="Enter Email Address" name="email" required>

            <button type="submit"> Password Reset</button>
        </div>

        <!-- Back to Login Link -->
        <div class="back-to-login">
            <a href="login.html">Back to Login</a>
        </div>
    </form>

</body>
</html>
